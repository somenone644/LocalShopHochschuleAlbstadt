Server läuft auf Port 8000:>> npm run server
Client läuft auf Port 3000:>> npm run client

newShop.html soweit fertig:
Nur eingeloggte Nutzer können Daten absenden.
Beim Array, welches alle Formdata-Objekte enthält, werden je nach Art des Formulars
die services adresse.js,produkte.js und uploadmehrere.js genutzt.
Alle Daten werden an die Datenbank gesendet.
Es fehlen noch Methoden zur Bestätigung, löschen und verändern von Einträgen.

profile.html unvollständig:
abhängig von der benutzerid sollte die Profilseite geladen werden. 
Ich habe nur beispielhaft die Strasse des Hofladens, 
welchem dem aktuellen Benutzer zugeordnet wird in das Html-Dokument eingefügt.

register.html fast fertig:
es können allerdings identische accounts angelegt werden.

login.html:
sollte funktionieren.
